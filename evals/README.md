# Evaluation design

Reproducible cases retain chain/block provenance, RPC fixtures and expected evidence IDs, findings and scores. Synthetic edge cases are labelled synthetic.

Evaluate extraction, exact signals, scoring stability, agent citation validity and unsupported-claim rejection separately. Deterministic evaluation runs offline in CI; live agent evaluation is opt-in and budget-bounded.